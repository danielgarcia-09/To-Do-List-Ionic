import { FiltroCompletePipe } from './filtro-complete.pipe';

describe('FiltroCompletePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCompletePipe();
    expect(pipe).toBeTruthy();
  });
});
